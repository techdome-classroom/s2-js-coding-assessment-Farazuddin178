/**
 * @param {string} s
 * @return {boolean}
 */
var isValid = function(s) {
    const bracketMap={
        ')':'(',
        '}':'{',
        ']':'['
    }
    const stack=[];
    for (let char of s)
};

module.exports = { isValid };


